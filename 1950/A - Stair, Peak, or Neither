#include <bits/stdc++.h>
using namespace std;
 
int main(){
  int t;
  cin >> t;
  while(t--){
    int a,b,c;
    cin >> a >> b >> c;
    string stair = "STAIR";
    string peak = "PEAK";
    string none = "NONE";
    if ((a<b) && (b<c)){
      cout << stair << endl;
    }
    else if ((a<b) && (b>c)){
      cout << peak << endl;
    }
    else {
      cout << none << endl;
    }
  }
}